#pragma once

enum FishingPoleType
{
    SmallPole,
    MediumPole,
    BigPole
};

enum BaitType
{
    RedBait,
    BlueBait,
    GreenBait
};

enum FishColor
{
    Red,
    Blue,
    Green
};

enum FishSize
{
    Small,
    Medium,
    Big
};

enum GameState
{
    Default,
    Win,
    Lose,
    Tie
};